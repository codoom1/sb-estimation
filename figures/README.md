# Figures

Figure outputs are grouped by purpose.

- `manuscript/` contains final figures referenced by the manuscript.
- `workflow/` contains preprocessing workflow variants and development versions.
- `diagnostics/` contains cutoff-selection and objective-function diagnostics.
- `exploratory/` contains supporting exploratory and small-panel figures.

The main manuscript render writes final figures to `figures/manuscript/`.
